XAI_API_KEY=
ANTHROPIC_API_KEY=

# Owner mode: set to "true" in your private deploy to skip onboarding
# and use the built-in voice spec (lib/prompts/voice-spec.ts) for all generations.
# Leave unset (or "false") for the public version.
OWNER_MODE=
